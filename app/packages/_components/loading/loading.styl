@import '{gq:base-styles}/variables'

.loading
  position absolute
  top 45%
  left 50%
  transform translate(-50%, -50%)
  color $m-gray
  i
    font-size 8em
    &.small
      font-size 4em
    &.space-top
      margin-top 4em
