template(name="form_results")
  .results
    .results--content
      +list_flank(
        collections=formCollection
        selected=selectedFormIds
        selectMultiple=true
        location='left')
      .results--results
        .forms-results
          each forms
            .forms-results--result
              p=title
