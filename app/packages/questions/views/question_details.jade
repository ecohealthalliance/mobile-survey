template(name="question_details")
  with question
    if surveyIsActive
      h4=text
    else
      +question_details_edit(
        surveyId=../survey.id
        formId=../formId
        question=question
        choices=choices
        type=type)
  else
    +loading
