template(name="form_results_detail")
  each questions
    .question-results
      .question-results--title
        h4=text
      .question-results--details
        if answers
          +basic_results_info type=type props=properties particpation=particpation
          +Template.dynamic template=template data=templateData
        else
          p No Submissions
