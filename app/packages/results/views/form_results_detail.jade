template(name="form_results_detail")
  each questions
    .question-results
      .question-results--title
        h5=text
      if answers
        +Template.dynamic template=type data=templateData
      else
        p No Submissions
