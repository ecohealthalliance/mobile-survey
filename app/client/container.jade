template(name="container")
  .container
    .row
      p
      unless hasForms
        h2 Create a new form:
          +quickForm(collection="Forms" type="insert" id="forms_insert")
      +forms

template(name="form")
  .col-xs-6
    h3 Resulting Form
    +quickForm(schema=schema id=_id)
  .col-xs-6
    h3 Form Builder
    +quickForm(doc=this collection="Forms" type="update" id=updateFormId)

template(name="forms")
  each forms
    +form
